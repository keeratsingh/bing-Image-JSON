<Page
    x:Class="bingImage.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:bingImage"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <ScrollViewer ScrollViewer.VerticalScrollBarVisibility="Auto">
        <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    
            <StackPanel Orientation="Vertical" Margin="10">
                <TextBlock Text="Bing image of the day" 
                           Style="{ThemeResource SubtitleTextBlockStyle}"
                           HorizontalAlignment="Center"/>
                <Slider x:Name="sldNoOfImages" Margin="10,0,10,-15"
                        Value="4" Minimum="1" Maximum="8" 
                        ValueChanged="sldNoOfImages_ValueChanged"/>
                <TextBlock Text="# of Images" HorizontalAlignment="Center" />
                <StackPanel x:Name="spImages" Orientation="Vertical"
                            Margin="10"/>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>
